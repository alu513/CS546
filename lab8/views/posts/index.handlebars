{{#if foundSomeone}}

<h1>People Found</h1>
<h2>{{personName}}</h2>
<main>
    <ul>
        {{#each foundSomeone}}
        <li><a href="/details/{{this.id}}">{{this.firstName}} {{this.lastName}}</a></li>
        {{/each}}
    </ul>
    <a href="/">Make another search</a>
</main>

{{else}}
<p class="not-found">We're sorry, but no results were found for {{personName}}.</p>
{{/if}}